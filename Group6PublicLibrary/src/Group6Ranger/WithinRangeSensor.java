package Group6Ranger;
/**
 * concrete subject which detects when an object is within a certain range of the sensor
 */
public final class WithinRangeSensor extends RangeSensor {
    
    public WithinRangeSensor(int pin) {
        super(pin);
    }
}
